{
    "name": "yosei-sql-formatter",
    "version": "1.0.0",
    "description": "SQL Query Formatter Web App",
    "scripts": {
        "start": "node script.js",
        "build": "parcel build index.html --out-dir dist",
        "deploy": "netlify deploy --prod"
    },
    "dependencies": {
        "sql-formatter": "^3.1.0"
    },
    "devDependencies": {
        "parcel-bundler": "^1.12.5"
    }
}
